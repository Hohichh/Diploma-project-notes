<!-- Опираясь на наш предыдущий критический анализ и исходные требования к системе, раздел 3 действительно нуждается в реструктуризации. Текущий вариант хорошо описывает базовую логику (CRUD, транзакции, базовый алгоритм), но упускает то, что делает вашу систему **интеллектуальной**.

Вот подробный план реструктуризации раздела 3, который сместит фокус на нетривиальные задачи (LLM, RAG, эвристики) и сделает демонстрацию завершенной.

---

### План реструктуризации раздела 3 «Реализация персонального ассистента...»

#### 3.1 Обоснование технических и программных средств

**Цель подраздела:** Кратко и аргументированно объяснить выбор стека, опираясь на архитектуру.

* **Что оставить:** * Выбор Java 21, Spring Boot, React, Docker (оставить в текущем сжатом виде).
* Выбор PostgreSQL (ограничения исключения — отличный аргумент, оставляем).


* **Что добавить:**
* **Выбор конкретной LLM:** Уточнить, какая именно модель используется через OpenRouter (например, `gpt-4o-mini` или `Claude 3.5 Haiku`), обосновав это поддержкой режима `Structured Outputs` и размером контекстного окна (для вмещения RAG-фрагментов).
* **Обоснование векторного хранилища:** Дополнить абзац про PostgreSQL упоминанием расширения `pgvector`, так как это избавляет от необходимости поднимать отдельную векторную БД (например, Milvus или Pinecone) и позволяет хранить реляционные данные и эмбеддинги в единой инфраструктуре.


* **Что убрать/сократить:** * Общие слова о том, что такое JWT или Axios (комиссия это знает, достаточно просто упомянуть их использование). -->

#### правки по 3.1

добавить источники возле ссылки на таблицу
GPT-4o-mini (openai/gpt-4o-mini) — Спецификации, бенчмарки и цены: https://openrouter.ai/openai/gpt-4o-mini

Claude 3.5 Haiku (anthropic/claude-3.5-haiku) — Спецификации и цены: https://openrouter.ai/anthropic/claude-3.5-haiku

DeepSeek V3 (deepseek/deepseek-chat) — Спецификации и агрегированная статистика провайдеров: https://openrouter.ai/deepseek/deepseek-chat

поправить саму таблицу
поправить листинг конфига - он на двух страницах

#### 3.2 Обзор результатов реализации
<!-- 
**Цель подраздела:** Раскрыть «подкапотную» магию самых сложных алгоритмов системы. Убираем фокус с контроллеров и переносим на ИИ и алгоритмы.

* **3.2.1 Алгоритм планирования временных слотов (Оставить и расширить)**
* Оставляем псевдокод (рис. 3.1). Он отлично показывает детерминированную часть.
 -->

* **3.2.2 Обработка структурированного вывода языковой модели (Оставить)**
* Оставляем текущее описание парсинга JSON и код (рис. 3.2). - вот тут мб форматнуть покрасивее, либо псевдо код, либо другой кусок кода. а то там не совсем понятно, что делается.


<!-- * **3.2.3 Реализация подсистемы семантического поиска и RAG (НОВОЕ)**
* *Это ядро вашей научной новизны.* Здесь нужно описать, как методичка превращается в контекст:
* Процесс чанкинга (разбиения текста на фрагменты, например, по 500 токенов с перекрытием).
* Вычисление эмбеддингов (какая модель переводит текст в вектор).
* Работа метода `similaritySearch` (поиск `topK` релевантных фрагментов с использованием метрики косинусного сходства через `pgvector`).


* **3.2.4 Транзакционное управление жизненным циклом черновика (Оставить)**
* Оставить текущий текст про атомарность, статусы подзадач и отложенное удаление. -->



#### 3.3 Демонстрация работоспособности системы

**Цель подраздела:** Доказать через UI, что все заявленные в Главе 1 и 2 проблемы (прокрастинация, когнитивная перегрузка, планирование) решены.

* **Что оставить:**
* Базовый флоу: Главный экран $\rightarrow$ Генерация черновика $\rightarrow$ Ручная правка $\rightarrow$ Подтверждение / Отклонение $\rightarrow$ Нехватка времени (Edge case).


* **Что ОБЯЗАТЕЛЬНО добавить (новые сценарии тестирования):**
* **Сценарий 1: Работа с контекстом (Демонстрация RAG).** * *Как показать:* Пользователь загружает PDF-файл (например, «Методические указания к ЛР №1»). В чате просит: *"Составь план по загруженной методичке"*. Агент выдает не абстрактные шаги, а **специфические** (например: *"Шаг 1: Настроить маршрутизацию OSPF в Packet Tracer"*).
* **Сценарий 2: Маркеры когнитивной нагрузки.**
* *Как показать:* Обратить внимание в тексте (и на скриншотах), что у сгенерированных карточек задач в UI появились цветовые маркеры или иконки (Высокая/Средняя/Низкая нагрузка), которые LLM вернула в JSON. Это доказывает реализацию «академического контекста».


* **Сценарий 3: Выполнение задачи и борьба с прокрастинацией.**
* *Как показать:* Скриншот, где пользователь отмечает слот как «Выполненный». На панели справа заполняется прогресс-бар задачи (например, $1/3 \rightarrow 2/3$) и увеличивается счетчик «Дней в ударе» (streak). Это логически закрывает блок из Главы 1 про геймификацию и эффект Зейгарник.


---

### Резюме для вас:

Если вы перестроите текст по этому плану, ваша третья глава станет железобетонной. Она будет начинаться с выбора современных инструментов (`pgvector`, `Structured Outputs`), переходить к математике и алгоритмам (RAG, жадный алгоритм слотов), и заканчиваться визуальным доказательством того, что всё это работает в браузере.

С какого пункта начнем переписывать? Можем начать с написания нового подраздела **3.2.3 про реализацию семантического поиска (RAG)**.